# 文件夹中英文映射表

## 系统文件夹（英文名 → 中文名）

| 英文名 | 中文名 | 用途说明 | 用户可见 |
|--------|--------|----------|----------|
| `docs` | 文档 | 存放文档和笔记文件 | ✅ 是 |
| `images` | 图片 | 存放图片和媒体文件 | ✅ 是 |
| `projects` | 项目 | 存放各种项目文件 | ✅ 是 |
| `tools` | 工具 | 存放工具和脚本文件 | ✅ 是 |
| `temp` | 临时文件 | 存放临时文件 | ✅ 是 |

## 已创建的文件结构

```
C:\Users\czp\openclaw\
├── docs\                    # 文档文件夹
│   └── 说明.md
├── images\                  # 图片文件夹
│   └── 说明.md
├── projects\                # 项目文件夹
│   └── 说明.md
├── tools\                   # 工具文件夹
│   └── 说明.md
├── temp\                    # 临时文件夹
│   └── 说明.md
├── 文件夹命名规范.md        # 命名规则文档
├── 文件夹映射表.md          # 本文件
└── ... (其他文件)
```

## 如何使用

### 对我（小灵同学助理）说话时：
- 你可以用中文说："把文件放到文档文件夹"
- 我会自动理解你要放到 `docs` 文件夹
- 我回复时会用中文："已保存到文档文件夹"

### 文件操作示例：
1. **保存文档** → "保存到文档文件夹"
2. **存放图片** → "存到图片文件夹"  
3. **创建项目** → "在项目文件夹中创建新项目"
4. **临时文件** → "放到临时文件夹"

## 添加新文件夹

如果你需要新的文件夹类型，告诉我：
1. 文件夹的用途（中文）
2. 是否需要用户可见
3. 预期的文件类型

我会：
1. 创建英文文件夹（系统使用）
2. 添加中文说明文件
3. 更新这个映射表
4. 确保你看到的是中文名称

## 注意事项

1. **编码问题**：Windows有时显示中文文件夹名会有乱码，但实际文件操作正常
2. **路径安全**：避免在文件夹名中使用特殊字符
3. **大小写**：Windows不区分大小写，但建议保持一致性